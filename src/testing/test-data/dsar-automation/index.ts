import { dashboard } from './dashboard';
import { request } from './request';
import { tags } from './tags';
import { task } from './task';
import { webform } from './webform';
import { workflow } from './workflow';

export const dsarAutomation = {
  dashboard,
  request,
  tags,
  task,
  webform,
  workflow,
};
